const { expect } = require('chai');
const sinon = require('sinon');
const EventEmitter = require('events');

const EventWrapper = require("../src/index");

describe('Manipulating functions using wrapMethods function by extending functions', () => {
    describe('instance', () => {
        it('should wrap methods with event emitters', () => {
            
        });
    });
});
