const { expect } = require('chai');
const sinon = require('sinon');
const EventEmitter = require('events');

const EventWrapper = require("../src/index");

describe('Manipulating functions using getMethods function by extending classes', () => {
    describe('instance', () => {
        it('should getmethods with event emitters', () => {
            
        });
    });
});
