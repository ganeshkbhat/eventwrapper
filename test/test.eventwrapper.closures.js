const { expect } = require('chai');
const sinon = require('sinon');
const EventEmitter = require('events');

const MyFunctionWithEvents = require("../src/index");

describe('Manipulating closure functions using wrapMethods function by extending functions', () => {
    describe('instance', () => {
        it('should wrap methods with event emitters', () => {
            
        });
    });
});
